# Claude Code does not auto-load this file. Either:
#   1) copy to .env and `set -a && source .env && set +a` before running claude, or
#   2) export YNAB_API_TOKEN directly in your shell profile (~/.zshrc)
YNAB_API_TOKEN=paste-your-personal-access-token-here
