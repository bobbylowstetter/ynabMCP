{
  "mcpServers": {
    "ynab": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@maro-org/ynab-mcp"],
      "env": {
        "YNAB_API_TOKEN": "${YNAB_API_TOKEN}",
        "YNAB_READ_ONLY": "false",
        "NODE_USE_ENV_PROXY": "1"
      }
    }
  }
}